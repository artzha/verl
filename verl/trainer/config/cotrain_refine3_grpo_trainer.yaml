# Cotrain-refine3 GRPO trainer: shared actor generates ALL THREE trainable steps inside the
# agent loop — initial motion, critique, and refined motion — with two masked user-turn
# injections between them.
#
# Pipeline (per replica, no before_actor staging needed):
#   1. Initial motion generation          (response_mask=train_initial_motion, contributes to GRPO loss by default)
#   2. Critic prompt + annotated image    (injected as non-trainable context, response_mask=0)
#   3. Critique generation                (response_mask=1, trainable)
#   4. Motion-refine prompt               (injected as non-trainable context, response_mask=0)
#   5. Refined motion generation          (response_mask=1, trainable)
#
# The prompt batch is repeated rollout.n times BEFORE the agent loop runs (rollout_generations
# is empty), so each GRPO group member independently samples its own initial motion, critique,
# and refined motion. motion_response = [init_text, refined_text] so reward_type=relative
# works correctly (r_T - r_0 = refined_score - initial_score).
#
# Key differences vs cotrain_refine_grpo_trainer.yaml:
#   - rollout_generations.before_actor: []  (no pre-staging)
#   - actor_rollout_ref.rollout.generate_initial_motion: true
#   - actor_rollout_ref.rollout.train_initial_motion: true
#   - actor_rollout_ref.rollout.motion_response_length shared for both initial and refined motion
#   - actor_rollout_ref.rollout.response_length bumped to 1536 (covers all 5 spans)
#   - actor_rollout_ref.rollout.engine_kwargs.vllm.max_model_len bumped to 4096
#   - actor_rollout_ref.extra_rollouts: []  (no extra rollout servers needed)

# Format checks enforced on CI:
# 1. Comments must appear above each field.
# 2. There must be a blank line between each field.
# 3. Inline comments (after a field on the same line) are not allowed.
# 4. Indentation level is respected for nested fields.
defaults:

  # <folder_name>@<field_name>.<field_name>: <yaml_file_name>
  # actor_rollout_ref.actor: trainer/config/actor/dp_actor.yaml
  - actor@actor_rollout_ref.actor: dp_actor

  - data@data: motion_foresight

  # (Rule-based) Reward manager config.
  - reward_manager@reward_manager

  # Reference model config.
  - ref@actor_rollout_ref.ref: dp_ref

  # Rollout model config.
  - rollout@actor_rollout_ref.rollout: rollout

  # Shared rollout config for all extra rollouts (none used in refine3, kept for schema compat).
  - rollout@actor_rollout_ref.extra_rollout_rollout: rollout

  # Model config.
  - model@actor_rollout_ref.model: hf_model

  # Critic model config.
  - critic@critic: dp_critic

  # Reward model config.
  - reward_model@reward_model: dp_reward_model

  # Rollout correction config.
  - algorithm@algorithm.rollout_correction: rollout_correction

  # load the reference default config, then apply the fields in the current yaml
  # self config override anything above
  - _self_

# prompts
motion_prompt_path: 'cotnav/prompts/datagen/plan_v4/motion_prompt.txt'

motion_refine_prompt_path: 'cotnav/prompts/datagen/plan_v4/motion_refine_prompt.txt'

critic_prompt_path: 'cotnav/prompts/datagen/plan_v4/critic_prompt.txt'

actor_rollout_ref:
  hybrid_engine: true

  critic_output_format: verdict:v1

  critic_prompt_path: ${critic_prompt_path}

  external_worker:
    path: cotnav/verl_adapters/workers/hybrid_motion_actor.py
    name: HybridMotionActorRolloutRefWorker

  actor:
    strategy: fsdp2
    fsdp_config:
      model_dtype: bf16
      strategy: fsdp2
      # param_dtype=bf16: forward pass in bf16 (base + LoRA all-gathered and cast to bf16)
      # reduce_dtype=fp32: gradient reduction in fp32 for numerical stability
      # output_dtype=fp32: sharded gradient cast to fp32 before assignment to fp32 LoRA master weights
      # LoRA A/B params remain fp32 (PEFT default); optimizer states remain fp32
      mixed_precision:
        param_dtype: fp32
        output_dtype: fp32
        reduce_dtype: fp32
        buffer_dtype: fp32

    freeze_vision_tower: False

  # Ref policy: forward-only, no gradients — bf16 throughout, no output_dtype needed
  ref:
    strategy: fsdp2
    fsdp_config:
      model_dtype: bf16
      strategy: fsdp2
      mixed_precision:
        param_dtype: bf16
        output_dtype: bf16
        reduce_dtype: fp32
        buffer_dtype: fp32

    freeze_vision_tower: False

  model:
    # Preloads model weights to /dev/shm to improve model loading speed
    # Do not use is RAM is limited (causes OOM)
    use_shm: False

    # Whether to use custom fused kernels (PostProcessing, for memory efficiency)
    use_fused_kernels: False

    trust_remote_code: False

    # Whether to remove padding tokens in inputs during training
    use_remove_padding: True

    # LoRA knobs (VeRL infra) See model/hf_model.yaml for definitions
    lora_rank: 0.0

    lora_alpha: 0.0

    target_modules: all-linear

    exclude_modules: null


  rollout:
    name: vllm

    mode: async

    # Return rollout_log_probs during generation so bypass_mode can skip expensive old_log_prob recompute.
    calculate_log_probs: true

    temperature: 1.0

    top_p: 0.95

    prompt_length: 1680

    # Budget covers all 5 spans:
    #   initial motion (~192) + critic injection (~500, incl. ann_img tokens) +
    #   critique (~256) + refine injection (~120) + refined motion (~192) ≈ 1260.
    # 1536 provides comfortable headroom.
    response_length: 1536

    # Per-turn response length caps.
    # critique_response_length: critique step (step 3).
    critique_response_length: 256

    # motion_response_length: used for both the initial motion step (step 1) and the refined
    # motion step (step 5). Both generation calls share this same token budget.
    motion_response_length: 192

    # Whether to generate initial motion as a trainable step inside the agent loop.
    # True: three-step mode (no before_actor needed).
    # False: two-step mode (before_actor stages initial motion in the prompt).
    generate_initial_motion: true

    # Whether to include the initial motion span in the GRPO loss (response_mask=1).
    # Set to false to mask it out while still using it for critic context.
    train_initial_motion: true

    # Must be null: the agent loop builds motion_response/critic_response in extra_fields
    # directly. Setting "motion" here would append the full combined response and corrupt RM scoring.
    response_role: null

    engine_kwargs:
      vllm:
        # Increased from 2560 to accommodate prompt_length + response_length = 1680 + 1536 = 3216.
        max_model_len: 4096
        max_num_seqs: 64
        max_num_batched_tokens: 8192
        gpu_memory_utilization: 0.9

    # Required: Allows vllm to load base model
    load_format: safetensors

    # Agent loop drives all three shared-actor generations with two injected user turns between them.
    agent:
      # Route all samples to the cotrain-refine loop unless `agent_name` is set per-sample.
      default_agent_loop: cotrain_refine_agent
      # Points to the hydra registry entry that instantiates CotrainRefineAgentLoop.
      agent_loop_config_path: cotnav/verl_adapters/agent_loops/agent_loop_config.yaml

  # No before_actor staging: the agent loop generates the initial motion internally.
  # The prompt batch is repeated rollout.n times by the trainer, then each replica runs the
  # full 3-step agent loop independently.
  rollout_generations:
    before_actor: []

  # Shared rollout config for all extra rollouts (unused in refine3, kept for schema compat).
  extra_rollout_rollout:
    name: vllm
    mode: async

    # Sampling parameters
    temperature: 1.0
    top_p: 0.95
    prompt_length: 1680
    response_length: 256

    tensor_model_parallel_size: 2
    enable_chunked_prefill: true
    enable_prefix_caching: true

    engine_kwargs:
      vllm:
        max_model_len: 2048
        max_num_seqs: 64
        gpu_memory_utilization: 0.9
        dtype: bfloat16
        max_num_batched_tokens: 8192
        swap_space: 0

  # No extra rollout servers needed: the shared actor handles all generations in-loop.
  extra_rollouts: []

reward_model:
  enable: true

  use_reward_loop: false

  strategy: fsdp2

  external_worker:
    path: cotnav/verl_adapters/workers/qwen3vl_reward_worker.py
    name: Qwen3VLMotionRewardWorker

  model:
    path: null
    external_lib: verl.models.transformers.qwen3_vl_reward
    trust_remote_code: true
    use_remove_padding: true
    apply_chat_template_kwargs: {
      do_sample_frames: false,
      video_metadata: {
        fps: 0.5,
        frames_indices: [0, 1, 2, 3],
        total_num_frames: 4
      }
    }

  use_dynamic_bsz: true

  # Per-segment format penalty weights applied on top of learned RM scores in
  # _apply_cotrain_format_penalty (ray_trainer.py). Set to 0.0 to disable.
  # Kept under a separate key so load_reward_manager does not forward these to
  # the NaiveRewardManager constructor via **reward_kwargs.
  format_penalty:
    critic_format_penalty: 0.5
    motion_format_penalty: 0.5

  # ------------------------------------------------------------------
  # Reward components blended in RayPPOTrainer._compute_motion_rm_score.
  # Two parallel blocks (rm = learned reward model, mdist = motion-distance
  # vs ground-truth trajectory) share the same reward_type / weight schema.
  #
  # reward_type values, with their per-block component mapping:
  #   absolute       -> w[0] * absolute_scalar
  #     rm.absolute    = r_T  (RM score on the final refined motion)
  #     mdist.absolute = abs_final_score  (final pred vs GT, in [0.5, 1.0])
  #   relative       -> w[0] * relative_scalar
  #     rm.relative    = r_T - r_0  (improvement)
  #     mdist.relative = rel_score  ((d_prev - d_curr)/sqrt(2) in [-1, 1])
  #   delta_initial  -> w[0] * relative_scalar + w[1] * initial_scalar
  #     rm.initial    = r_0
  #     mdist.initial = abs_initial_score  (initial pred vs GT, in [0.5, 1.0])
  #   delta_absolute -> w[0] * relative_scalar + w[1] * absolute_scalar
  #     same scalars as `relative` + `absolute` above; this variant rewards
  #     the per-step improvement together with the final (post-refinement)
  #     reward, instead of the initial reward used by delta_initial.
  #
  # When 2+ weighted components survive across both blocks, every term is
  # std-normalized per-batch (eps=1e-4 clamp) so the configured weights act
  # on a comparable scale; a single-term blend passes through unscaled. With
  # generate_initial_motion=true motion_response = [init_text, refined_text]
  # so r_0 / abs_initial_score are well-defined for all reward_types.
  # ------------------------------------------------------------------

  # Learned reward-model branch.
  rm:

    # absolute | relative | delta_initial | delta_absolute.
    reward_type: relative

    # 2-element weight vector. [0] applies to the primary scalar (absolute
    # for reward_type=absolute, relative otherwise). [1] only kicks in for
    # the delta_* variants and weights the secondary scalar:
    #   delta_initial  -> [1] weights initial  (r_0)
    #   delta_absolute -> [1] weights absolute (r_T)
    # Set the unused slot to anything; it is filtered out when its value is 0.0.
    rm_weight: [1.0, 0.0]

  # Auxiliary ground-truth-conditioned motion-distance branch. Disabled by
  # default (both mdist_weight slots at 0.0) so no extra compute / blending
  # happens and rm_scores reproduce the RM-only path bit-identically.
  mdist:

    # absolute | relative | delta_initial | delta_absolute.
    reward_type: absolute

    # Distance metric used to compute abs_final / rel / abs_initial against
    # extra_info.trace_pts. One of: directed_hdist | sym_hdist | final_l2 | dtw.
    metric: directed_hdist

    # Same indexing convention as rm.rm_weight.
    mdist_weight: [0.0, 0.0]

reward_manager:
  name: naive

# custom reward function definition
custom_reward_function:

  # The path to the file containing your customized reward function.
  # If not specified, pre-implemented reward functions will be used.
  path: null

  # The name of the reward function within the specified file. Default is 'compute_score'.
  name: null

# config for the algorithm
algorithm:

  # Required when using verl.utils.omega_conf_to_dataclass to instantiate dataclass configs
  _target_: verl.trainer.config.AlgoConfig

  # Discount factor for future rewards
  gamma: 1.0

  # Trade-off between bias and variance in the GAE estimator
  lam: 1.0

  # Rollout correction: bypass_mode uses rollout_log_probs as old_log_probs (skips ~425s actor forward).
  rollout_correction:
    bypass_mode: true

  # Advantage estimator type: "gae", "grpo", "reinforce_plus_plus", etc.
  adv_estimator: grpo

  # Whether to normalize advantages by std (specific to GRPO)
  norm_adv_by_std_in_grpo: True

  # Whether to use process-level GRPO (token-level credit assignment)
  use_process_credit: False

  # Whether to enable in-reward KL penalty
  use_kl_in_reward: False

  # How to estimate KL divergence: "kl", "abs", "mse", "low_var_kl", or "full"
  kl_penalty: kl

  # KL control configuration
  kl_ctrl:

    # Required when using verl.utils.omega_conf_to_dataclass to instantiate dataclass configs
    _target_: verl.trainer.config.KLControlConfig

    # KL control type: "fixed" or "adaptive"
    type: fixed

    # Initial coefficient for KL penalty
    kl_coef: 0.001

    # Horizon value for adaptive controller (if enabled)
    horizon: 10000

    # Target KL divergence (used for adaptive controller)
    target_kl: 0.1

  # Whether to enable preference feedback PPO
  use_pf_ppo: False

  # Preference feedback PPO settings
  pf_ppo:

    # Method for reweighting samples: "pow", "max_min", or "max_random"
    reweight_method: pow

    # Power used for weight scaling in "pow" method
    weight_pow: 2.0

trainer:
  # Whether to balance batch sizes across distributed workers
  balance_batch: True

  # Number of epochs in training
  total_epochs: 30

  # Total training steps (can be set explicitly or derived from epochs)
  total_training_steps: null

  # Project name for experiment tracking (e.g., wandb)
  project_name: autorefine-critic

  # Experiment name for run identification in tracking tools
  experiment_name: qwen3_vl_2b_fsdp_cotrain_refine3_${reward_model.reward_type}

  # Logging backends to use: "console", "wandb", etc.
  logger: ["console", "wandb"]

  # Number of generations to log during validation
  log_val_generations: 0

  # Directory for logging rollout data; no dump if null
  rollout_data_dir: null

  # Directory for logging validation data; no dump if null
  validation_data_dir: null

  # Number of nodes used in the training
  nnodes: 1

  # Number of GPUs per node
  n_gpus_per_node: 4

  # Save frequency (by iteration) for model checkpoints
  save_freq: -1

  # ESI refers to the elastic server instance used during training, similar to the training plan. For example,
  # if you purchase 10 hours of computing power, the ESI will automatically shut down after 10 hours of training.
  # To ensure a checkpoint is saved before ESI shuts down, the system will start saving a checkpoint in advance.
  # The advance time is calculated as: Advance Time = Longest historical step duration + Checkpoint save duration + esi_redundant_time.
  # Here, esi_redundant_time is a user-defined value that further extends the advance time for added safety.
  esi_redundant_time: 0

  # Resume mode: "auto", "disable", or "resume_path"
  # "auto": resume from last checkpoint if available
  # "disable": start from scratch
  # "resume_path": resume from a user-defined path
  resume_mode: auto

  # Path to resume training from (only used when resume_mode is "resume_path")
  resume_from_path: null

  # Whether to run validation before training begins
  val_before_train: True

  # Whether to run validation only
  val_only: False

  # Validation frequency (in training iterations)
  test_freq: -1

  # Number of iterations to warm up the critic before updating policy
  critic_warmup: 0

  # Default path to distributed filesystem for saving checkpoints
  default_hdfs_dir: null

  # Whether to delete local checkpoints after loading
  del_local_ckpt_after_load: False

  # Default local directory for saving checkpoints
  default_local_dir: checkpoints/${trainer.project_name}/${trainer.experiment_name}

  # Maximum number of actor checkpoints to keep
  max_actor_ckpt_to_keep: null

  # Maximum number of critic checkpoints to keep
  max_critic_ckpt_to_keep: null

  # Timeout (in seconds) for Ray worker to wait for registration
  ray_wait_register_center_timeout: 300

  # Device to run training on (e.g., "cuda", "cpu")
  device: cuda

  # whether to use legacy worker implementation
  #  mode: "auto", "enable", or "disable"
  use_legacy_worker_impl: disable

# profiler configs
global_profiler:

  # Required when using verl.utils.omega_conf_to_dataclass to instantiate dataclass configs
  _target_: verl.utils.profiler.ProfilerConfig

  # Profiling tool: choose between nsys, npu, torch, torch_memory
  tool: nsys

  # profile steps
  steps: [2, 3]

  # Whether to combine continuous steps into one database.
  ## If True, worker.profiler.discrete must be False, [1,2] in one, [5] in another.
  ## If False, [1] in one, [2] in another, [5] in another.
  profile_continuous_steps: False

  # Path to save profiling contents
  save_path: "outputs/profile"

  # Specific tool configs, can use +profiler.tool_config.[tool].xxx to config
  global_tool_config:

    # nsys config
    nsys:

      # Required when using verl.utils.omega_conf_to_dataclass to instantiate dataclass configs
      _target_: verl.utils.profiler.config.NsightToolConfig

      # True for each task has its own database, False for all tasks in one training step share one database.
      discrete: False

      # controller Nvidia Nsight Systems Options. Must set when profile_steps is not None.
      ## reference https://docs.nvidia.com/nsight-systems/UserGuide/index.html
      ## reference https://docs.ray.io/en/latest/ray-observability/user-guides/profiling.html
      controller_nsight_options:

        # Select the API(s) to be traced.
        trace: "cuda,nvtx,cublas,ucx"

        # Track the GPU memory usage by CUDA kernels. Must be string type "true" or "false".
        cuda-memory-usage: "true"

        # CUDA graphs will be traced as a whole
        cuda-graph-trace: "graph"

      # worker Nvidia Nsight Systems Options. Must set when profile_steps is not None.
      worker_nsight_options:

        # Select the API(s) to be traced.
        trace: "cuda,nvtx,cublas,ucx"

        # Track the GPU memory usage by CUDA kernels. Must be string type "true" or "false".
        cuda-memory-usage: "true"

        # CUDA graphs will be traced as a whole
        cuda-graph-trace: "graph"

        # Profiling only in a range of torch.cuda.profiler.start and stop. Do not change this config.
        capture-range: "cudaProfilerApi"

        # Specify the desired behavior when a capture range ends.
        # In verl we need the torch.cuda.profiler.start/stop pair to repeats n times.
        # valid values are "repeat-shutdown:n" or null.
        # For normal whole step profiling, n = len(profile_steps);
        # but for discrete profiling, n = len(profile_steps) * Number(subtasks).
        # Or you can just leave it null and the program will use n = len(profile_steps) * 6;
        capture-range-end: null

        # Send signal to the target application's process group. We let the program to exit by itself.
        kill: none

    # enable memory visualization for debugging memory usage
    torch_memory:

      #  Maximum number of allocation entries to record
      trace_alloc_max_entries: 100_000

      # The depth of the call stack to capture for each allocation
      stack_depth: 32

      # 'alloc': records only allocation events || 'state': records memory state changes || 'all': records both.
      context: "all"

      # 'python': records Python stacks || 'cpp': records C++ stacks (available in some versions) || 'all': records both.
      stacks: "all"

      # devices, record_context etc.
      kw_args: {}

# configs for TransferQueue
transfer_queue:

  # Whether to enable transfer queue
  enable: False

# configs related to ray
ray_kwargs:

  # configs related to ray initialization
  ray_init:
    num_cpus: 32 # Number of CPUs for Ray. Use a fixed number instead of null when using SLURM.
    include_dashboard: True
    dashboard_host: 127.0.0.1
    dashboard_port: 8265

  # Path to save Ray timeline JSON for performance profiling
  timeline_json_file: null
