# DPO trainer config — online iterative DPO with on-policy critique generation.
#
# Inherits the motion_trainer defaults and overrides only DPO-specific fields.
# Sequence ordering (B3):
#   [sys][motion_prompt+obs] -> [motion_gen] -> [annotated_obs+critic_prompt]
#   -> [critique_gen] -> [refine_prompt] -> [chosen | rejected motion]
#
# response_mask covers only the final motion tokens; all upstream context is
# excluded from the DPO logp sum.
defaults:

  - data@data: motion_reward_foresight

  - actor@actor_rollout_ref.actor: dp_actor

  - reward_manager@reward_manager

  # DPO always needs a reference policy
  - ref@actor_rollout_ref.ref: dp_ref

  - rollout@actor_rollout_ref.rollout: rollout

  - rollout@actor_rollout_ref.extra_rollout_rollout: rollout

  - model@actor_rollout_ref.model: hf_model

  # Critic and reward model are disabled for DPO; still include defaults so
  # Hydra schema validation passes, but need_critic() / add_reward_model_worker()
  # short-circuit on algorithm.objective=dpo.
  - critic@critic: dp_critic

  - reward_model@reward_model: dp_reward_model

  - _self_

# Prompt paths (same as motion_trainer)
motion_prompt_path: 'cotnav/prompts/datagen/plan_v3/motion_prompt_startgoal.txt'
motion_refine_prompt_path: 'cotnav/prompts/datagen/plan_v3/motion_refine_prompt.txt'
critic_prompt_path: 'cotnav/prompts/datagen/plan_v3/critic_prompt.txt'

# data:
#   shuffle: true
#   seed: 42
#   train_files: null
#   val_files: null
#   train_batch_size: 64
#   val_batch_size: 32
#   max_length: 4096
#   # Passed to processor.apply_chat_template
#   apply_chat_template_kwargs:
#     do_sample_frames: false
#     video_metadata:
#       fps: 0.5
#       frames_indices: [0, 1, 2, 3]
#       total_num_frames: 4

# mandatory for bypassing reward model
reward_model:
  use_reward_loop: false

actor_rollout_ref:
  hybrid_engine: true
  critic_prompt_path: ${critic_prompt_path}
  motion_refine_prompt_path: ${motion_refine_prompt_path}

  external_worker:
    path: cotnav/verl_adapters/workers/hybrid_motion_actor.py
    name: HybridMotionActorRolloutRefWorker

  ref:
    fsdp_config:
      model_dtype: bf16
      mixed_precision:
        param_dtype: bf16
        reduce_dtype: fp32
        buffer_dtype: fp32

  actor:
    strategy: fsdp2

    # DPO objective: set loss_mode=dpo so the registered compute_policy_loss_dpo
    # function is dispatched in losses.py / dp_actor.py.
    policy_loss:
      loss_mode: dpo
      # Beta controls the KL-divergence strength in the DPO loss.
      # Higher beta = stay closer to reference policy.
      dpo_beta: 0.1

  model:
    use_shm: True

    # Whether to use custom fused kernels (PostProcessing, for memory efficiency)
    use_fused_kernels: False

    trust_remote_code: False

    # Whether to remove padding tokens in inputs during training
    use_remove_padding: True

    # LoRA knobs (VeRL infra) See model/hf_model.yaml for definitions
    lora_rank: 0.0
    lora_alpha: 0.0
    target_modules: all-linear
    exclude_modules: '.*visual.*'
    # exclude_modules: '.*\(vision_tower\|mm_projector\|vision_model\|multi_modal_projector\|visual\)\(\..*\)\?.*'

    lora:
      merge: True

  rollout:
    name: vllm
    mode: async
    # Return rollout_log_probs during generation so bypass_mode can skip expensive old_log_prob recompute.
    calculate_log_probs: true
    temperature: 1.0
    top_p: 0.95
    prompt_length: 1680
    response_length: 256
    # If set to "critic" or "motion", the actor's generated response is stored in that key and merged into extra_info for reward.
    response_role: motion

    engine_kwargs:
      vllm:
        max_model_len: 2048
        max_num_seqs: 64
        max_num_batched_tokens: 8192
        gpu_memory_utilization: 0.9

    # Required: Allows vllm to load base model
    load_format: safetensors

    # Reduces peak GPU memory usage
    # layered_summon: True

  rollout_generations:
    # Stage A: generate initial motion plan (motion rollout + critic postfill)
    before_actor:
      - rollout: motion
        postfill: critic
      - rollout: critic
        postfill: motion

  # Shared rollout config for all extra rollouts (replaces deprecated motion_rollout).
  extra_rollout_rollout:
    name: vllm
    mode: async

    # Sampling parameters
    temperature: 1.0
    top_p: 0.95
    # prompt_length: 2560
    prompt_length: 1680
    response_length: 256

    tensor_model_parallel_size: 2
    enable_chunked_prefill: true
    enable_prefix_caching: true

    engine_kwargs:
      vllm:
        max_model_len: 2048
        max_num_seqs: 64
        gpu_memory_utilization: 0.9           
        dtype: bfloat16
        max_num_batched_tokens: 16384
        swap_space: 0

  extra_rollouts:
    - name: motion
      model:
        path: ${actor_rollout_ref.model.path}
        trust_remote_code: true
      rollout: ${actor_rollout_ref.extra_rollout_rollout}
      sync_with_actor: true
      prompt_paths:
        critic: ${critic_prompt_path}
        motion: ${motion_refine_prompt_path}
    - name: critic
      model:
        path: Qwen/Qwen3-VL-8B-Instruct
        trust_remote_code: true
      rollout: ${actor_rollout_ref.extra_rollout_rollout}
      sync_with_actor: false
      prompt_paths:
        critic: ${critic_prompt_path}
        motion: ${motion_refine_prompt_path}

# DPO algorithm settings
algorithm:
  _target_: verl.trainer.config.AlgoConfig

  # Signals fit() to dispatch to _fit_dpo() instead of the GRPO loop.
  objective: dpo

  # DPO-specific hyperparameters
  dpo:
    # KL-divergence regularisation strength (must match actor.policy_loss.dpo_beta)
    beta: ${actor_rollout_ref.actor.policy_loss.dpo_beta}

  # The following GRPO fields are unused by DPO but kept so AlgoConfig validates.
  gamma: 1.0
  lam: 1.0
  adv_estimator: grpo
  norm_adv_by_std_in_grpo: true
  use_kl_in_reward: false
  kl_penalty: kl
  kl_ctrl:
    _target_: verl.trainer.config.KLControlConfig
    type: fixed
    kl_coef: 0.001
    horizon: 10000
    target_kl: 0.1

# Critic disabled for DPO
critic:
  enable: false

trainer:
  # Whether to balance batch sizes across distributed workers
  balance_batch: True
  # Number of epochs in training
  total_epochs: 30
  # Total training steps (can be set explicitly or derived from epochs)
  total_training_steps: null
  # Project name for experiment tracking (e.g., wandb)
  project_name: verl_motion_dpo
  # Experiment name for run identification in tracking tools
  experiment_name: qwen3_vl_2b_fsdp
  # Logging backends to use: "console", "wandb", etc.
  logger: ["console", "wandb"]
  # Number of generations to log during validation
  log_val_generations: 0
  # Directory for logging rollout data; no dump if null
  rollout_data_dir: null
  # Directory for logging validation data; no dump if null
  validation_data_dir: null
  # Number of nodes used in the training
  nnodes: 1
  # Number of GPUs per node
  n_gpus_per_node: 4
  # Save frequency (by iteration) for model checkpoints
  save_freq: -1
  # ESI refers to the elastic server instance used during training, similar to the training plan. For example,
  # if you purchase 10 hours of computing power, the ESI will automatically shut down after 10 hours of training.
  # To ensure a checkpoint is saved before ESI shuts down, the system will start saving a checkpoint in advance.
  # The advance time is calculated as: Advance Time = Longest historical step duration + Checkpoint save duration + esi_redundant_time.
  # Here, esi_redundant_time is a user-defined value that further extends the advance time for added safety.
  esi_redundant_time: 0
  # Resume mode: "auto", "disable", or "resume_path"
  # "auto": resume from last checkpoint if available
  # "disable": start from scratch
  # "resume_path": resume from a user-defined path
  resume_mode: auto
  # Path to resume training from (only used when resume_mode is "resume_path")
  resume_from_path: null
  # Whether to run validation before training begins
  val_before_train: True
  # Whether to run validation only
  val_only: False
  # Validation frequency (in training iterations)
  test_freq: -1
  # Number of iterations to warm up the critic before updating policy
  critic_warmup: 0
  # Default path to distributed filesystem for saving checkpoints
  default_hdfs_dir: null
  # Whether to delete local checkpoints after loading
  del_local_ckpt_after_load: False
  # Default local directory for saving checkpoints
  default_local_dir: checkpoints/${trainer.project_name}/${trainer.experiment_name}
  # Maximum number of actor checkpoints to keep
  max_actor_ckpt_to_keep: null

  # Maximum number of critic checkpoints to keep
  max_critic_ckpt_to_keep: null

  # Timeout (in seconds) for Ray worker to wait for registration
  ray_wait_register_center_timeout: 300

  # Device to run training on (e.g., "cuda", "cpu")
  device: cuda

  # whether to use legacy worker implementation
  #  mode: "auto", "enable", or "disable"
  use_legacy_worker_impl: disable

# profiler configs
global_profiler:

  # Required when using verl.utils.omega_conf_to_dataclass to instantiate dataclass configs
  _target_: verl.utils.profiler.ProfilerConfig

  # Profiling tool: choose between nsys, npu, torch, torch_memory
  tool: nsys

  # profile steps
  steps: [2, 3]

  # Whether to combine continuous steps into one database.
  ## If True, worker.profiler.discrete must be False, [1,2] in one, [5] in another.
  ## If False, [1] in one, [2] in another, [5] in another.
  profile_continuous_steps: False

  # Path to save profiling contents
  save_path: "outputs/profile"

  # Specific tool configs, can use +profiler.tool_config.[tool].xxx to config
  global_tool_config:

    # nsys config
    nsys:

      # Required when using verl.utils.omega_conf_to_dataclass to instantiate dataclass configs
      _target_: verl.utils.profiler.config.NsightToolConfig

      # True for each task has its own database, False for all tasks in one training step share one database.
      discrete: False

      # controller Nvidia Nsight Systems Options. Must set when profile_steps is not None.
      ## reference https://docs.nvidia.com/nsight-systems/UserGuide/index.html
      ## reference https://docs.ray.io/en/latest/ray-observability/user-guides/profiling.html
      controller_nsight_options:

        # Select the API(s) to be traced.
        trace: "cuda,nvtx,cublas,ucx"

        # Track the GPU memory usage by CUDA kernels. Must be string type "true" or "false".
        cuda-memory-usage: "true"

        # CUDA graphs will be traced as a whole
        cuda-graph-trace: "graph"

      # worker Nvidia Nsight Systems Options. Must set when profile_steps is not None.
      worker_nsight_options:

        # Select the API(s) to be traced.
        trace: "cuda,nvtx,cublas,ucx"

        # Track the GPU memory usage by CUDA kernels. Must be string type "true" or "false".
        cuda-memory-usage: "true"

        # CUDA graphs will be traced as a whole
        cuda-graph-trace: "graph"

        # Profiling only in a range of torch.cuda.profiler.start and stop. Do not change this config.
        capture-range: "cudaProfilerApi"

        # Specify the desired behavior when a capture range ends.
        # In verl we need the torch.cuda.profiler.start/stop pair to repeats n times.
        # valid values are "repeat-shutdown:n" or null.
        # For normal whole step profiling, n = len(profile_steps);
        # but for discrete profiling, n = len(profile_steps) * Number(subtasks).
        # Or you can just leave it null and the program will use n = len(profile_steps) * 6;
        capture-range-end: null

        # Send signal to the target application's process group. We let the program to exit by itself.
        kill: none

    # enable memory visualization for debugging memory usage
    torch_memory:

      #  Maximum number of allocation entries to record
      trace_alloc_max_entries: 100_000

      # The depth of the call stack to capture for each allocation
      stack_depth: 32

      # 'alloc': records only allocation events || 'state': records memory state changes || 'all': records both.
      context: "all"

      # 'python': records Python stacks || 'cpp': records C++ stacks (available in some versions) || 'all': records both.
      stacks: "all"

      # devices, record_context etc.
      kw_args: {}

# configs for TransferQueue
transfer_queue:

  # Whether to enable transfer queue
  enable: False

# configs related to ray
ray_kwargs:

  # configs related to ray initialization
  ray_init:
    num_cpus: 16 # Number of CPUs for Ray. Use a fixed number instead of null when using SLURM.
    # object_store_memory: 21474836480 # 20 * 1024^3
    include_dashboard: True
    dashboard_host: 127.0.0.1
    dashboard_port: 8265

  # Path to save Ray timeline JSON for performance profiling
  timeline_json_file: "outputs/ray_timeline.json"
